/*DOM = DOCUMENT OBJECT MODEL 
        it is a object that represents the page you see in the web browser, and it provides you with an API to interact with it.
        Web browser constructs the DOM when it loads an HTML Document, and structures all the elements in a tree-like representation.
        Javascript can access the DOM to Dynamically change the content,structure,and style of a webpage.*/
        function darkMode(){

        document.body.style.backgroundColor="black"
        }
        function lightMode(){

        document.body.style.backgroundColor="white"
        }